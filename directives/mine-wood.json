{
  "name": "mine-wood",
  "title": "Mine wood",
  "description": "Chopped from whatever trees are nearest -- early poles and boxes. Hand-mined from the nearest source; wood comes back in its pockets.",
  "parameters": {
    "amount": {
      "default": 100,
      "description": "how much to come back with"
    },
    "reach": {
      "default": 512,
      "description": "how far to look; the map has to be explored that far"
    }
  },
  "steps": [
    {
      "do": "include",
      "directive": "gather",
      "parameters": {
        "resource": "tree",
        "kind": "type",
        "amount": "$amount",
        "reach": "$reach"
      }
    }
  ]
}
